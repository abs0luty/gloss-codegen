# Gloss

Gloss generates Gleam encoders and decoders from annotations so you can focus on modelling data instead of wiring JSON boilerplate. It understands project‑level configuration, file/type overrides, external helper functions.
